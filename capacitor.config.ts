import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.lostdutchmanmine.app",
  appName: "lost-dutchman-mine",
  webDir: ".webpack/renderer",
};

export default config;
